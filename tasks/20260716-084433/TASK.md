# docs: records live next to their task (RETRO/SPIKE/NOTES), archive stubs, convention in AGENTS.md

- STATUS: CLOSED
- PRIORITY: 0
- TAGS: docs

## Goal

Apply the nova-protocol docs restructure to this repo: move every task-scoped
record next to its task (`tasks/<id>/{RETRO,SPIKE,NOTES,REVIEW}.md`), leave
`docs/` holding only reference docs plus a `docs/retros/` bucket with the
`LESSONS.md` ledger and a few task-less records, recreate pruned task folders
as CLOSED archive stubs, rewrite all stale `docs/` path references, and
document the convention in `AGENTS.md` and `docs/README.md`.

See `NOTES.md` for what changed and `RETRO.md` for how it went.

