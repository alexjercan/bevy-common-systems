# docs: records live next to their task (RETRO/SPIKE/NOTES), archive stubs, convention in AGENTS.md

- STATUS: CLOSED
- PRIORITY: 0
- TAGS: docs

## Goal

Apply the nova-protocol docs restructure to this repo: move every task-scoped
record next to its task (`tasks/<id>/{RETRO,SPIKE,NOTES,REVIEW}.md`), leave
`docs/` holding only reference docs, `docs/plans/`, and the `docs/LESSONS.md`
ledger. Dissolve `docs/retros/` and `docs/spikes/` entirely - task-less
records become CLOSED archive stubs like the pruned tasks. Rewrite all stale
`docs/` path references, and document the convention in `AGENTS.md` and
`docs/README.md`.

See `NOTES.md` for what changed and `RETRO.md` for how it went.

