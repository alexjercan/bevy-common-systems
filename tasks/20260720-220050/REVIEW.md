# Review

## Round 1

- VERDICT: APPROVE
- REVIEWER: out-of-context

What I tried to break: I treated this as a hunt for a dishonest PROMOTED annotation - a lesson claiming a home that does not actually contain the guidance. I opened the worktree's AGENTS.md in full and cross-checked each domain lesson's claimed section against real text: `clippy-all-targets-gate` (Build/Verify/Run + Gotchas, the "bare cargo build skips examples" bullet), `order-against-a-real-edge` (Conventions "Ordering caveat"), `copy-bevy-019-ui-verbatim` (Gotchas "Bevy 0.19 UI/light API"), `headless-test-plugin-set` and `doctest-needs-state-or-hidden-fn` (Build/Verify/Run + Gotchas doctest/StatesPlugin text), `oracle-from-defining-library` (the from_rotation_arc / surface_frame gotcha), `verify-observable-effect` / `extract-pure-and-test-that` / `run-the-example` (the screenshot/pure-fn/run-the-example gotchas). All exist. I read /home/alex/AGENTS.md and confirmed the three process lessons annotated "-> global ~/AGENTS.md" are actually there: `full-command-output` (pipe-eats-exit-code rule), `pkill-by-pid` (kill by recorded PID), `evidence-before-claim` (edit is a hypothesis until the artifact shows it). I went further than required and located the skill files: review SKILL.md line 61 ("would it fail with the fix deleted?") backs `regression-test-must-fail`, work SKILL.md ("Start the task ... create the worktree with sprout") backs `sprout-first`, plan SKILL.md ("read the dependency's source ... before designing around it") backs `verify-api-in-source`. I ran the DoD commands: `promotion-stalled` count is 0; the only other findings are `closed-unchecked` on three unrelated CLOSED tasks (20260704-102342, 20260705-140043, 20260711-094942), explicitly out of scope per task 20260720-220102. `grep -c "Promoted ledger lessons" AGENTS.md` returns 1, and all 5 freshly-folded rules (reset-shared-state, input-driver-every-state, check-plugin-self-added-deps, harvest-by-reading-evidence, split-verifiable-from-manual) are present in that block, accurately worded and correctly placed at the end of the Conventions section. Pending promotions is now empty with a pointer to the annotations. I could not find a false promotion.

- No findings.
